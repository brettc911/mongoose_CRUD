<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Create Card</title>
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <h1>Create a new card</h1>
    <form class="cardForm" action="/saveCard" method=post>
      <div class="formField">
        <label for="name">Card Name</label>
        <input type="text" name="name" value={{card.name}}>
      </div>
      <div class="formField">
        <label for="type">Card type</label>
        <input type="text" name="type" value={{card.type}}>
      </div>
      <div class="formField">
        <label for="subType">Sub type</label>
        <input type="text" name="subType" value={{card.subType}}>
      </div>
      <div class="formField">
        <label for="manaCost">Mana cost</label>
        <input type="text" name="manaCost" value={{card.manaCost}}>
      </div>
      <div class="formField">
        <label for="action">Action: Full or Quick</label>
        <input type="text" name="action" value={{card.action}}>
      </div>
      <div class="formField">
        <label for="castRange">Cast Range</label>
        <input type="text" name="castRange" value={{card.castRange}}>
      </div>
      <div class="formField">
        <label for="target">Viable Targets</label>
        <input type="text" name="target" value={{card.target}}>
      </div>
      <button>Submit</button>
    </form>
  </body>
</html>
